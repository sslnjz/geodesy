@PACKAGE_INIT@

set(GEODESY_VERSION "@geodesy_VERSION@")
set_and_check(GEODESY_INCLUDE_DIR "@PACKAGE_CONFIG_INC_DIR@")
set_and_check(GEODESY_LIB_DIR "@PACKAGE_CONFIG_LIB_DIR@")

if (EXISTS "${CMAKE_CURRENT_LIST_DIR}/geodesy-targets.cmake")
    include ("${CMAKE_CURRENT_LIST_DIR}/geodesy-targets.cmake")
endif ()

check_required_components(geodesy)